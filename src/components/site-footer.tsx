export function SiteFooter() {
  return (
    <footer className="mt-16 border-t border-border/80">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-8 font-mono text-[11px] leading-relaxed text-muted-foreground sm:px-6 sm:flex-row sm:items-end sm:justify-between">
        <p>复述门 · 现场手册 01 · 模型不承担 on-call</p>
        <p>不需要新 Agent。需要你把理解重新当成稀缺资源。</p>
      </div>
    </footer>
  );
}
